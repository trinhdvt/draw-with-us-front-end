import Grid from "@mui/material/Grid";
import React from "react";

const Database = () => {
    return (
        <Grid item xs={0}>
            This is database
        </Grid>
    )
}

export default Database;